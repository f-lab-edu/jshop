spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      max-lifetime: 250000
  jpa:
    hibernate:
      ddl-auto: update

  cache:
    type: redis
    redis:
      time-to-live: 36000000

logging:
  level:
    sql: trace