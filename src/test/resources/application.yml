spring:
  application:
    name: jshop
  datasource:
    hikari:
      maximum-pool-size: 11
        #    url: jdbc:h2:tcp://localhost:1521/jshop;AUTO_SERVER=true;LOCK_TIMEOUT=1000000
      #    username: sa
      #    password:
    #    url: jdbc:mysql://localhost:3306/jshop?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    #    username: root
    #    password: 1234
    #    url: jdbc:postgresql://localhost:5432/jshop
    #    username: myuser
    #    password: mypassword
    p6spy:
      enable-logging: true
      multiline: true
      logging: slf4j
      tracing:
        include-parameter-values: true
  test:
    mockmvc:
      print: none
  jpa:
    #    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    properties:
      #      hibernate:
      #        dialect: org.hibernate.dialect.MySQLDialect
      #        dialect: org.hibernate.dialect.H2Dialect
      #        dialect: org.hibernate.dialect.PostgreSQLDialect
      org.hibernate:
        envers:
          audit_table_suffix: _history


  jwt:
    secret: 61dd10f1-cee2-454b-964f-9bda4cb993d861dd10f1-cee2-454b-964f-9bda4cb993d8
  data:
    jdbc:
      dialect: h2

logging:
  level:
    sql: error